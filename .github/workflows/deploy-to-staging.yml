name: Deploy to Staging

on:
  # Manual trigger
  workflow_dispatch:
  # Auto-deploy on push to develop branch (optional)
  # push:
  #   branches:
  #     - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t csodaszarvas-booking:staging .
          docker save csodaszarvas-booking:staging -o csodaszarvas-booking-staging.tar
          chmod 644 csodaszarvas-booking-staging.tar

      - name: Copy files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          source: "csodaszarvas-booking-staging.tar,docker-compose.staging.yml,scripts/deploy.sh"
          target: "/home/${{ secrets.VPS_USERNAME }}/apps/csodaszarvas-booking-staging/"

      - name: Fix deploy script path on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            cd ~/apps/csodaszarvas-booking-staging
            mv scripts/deploy.sh deploy.sh 2>/dev/null || true
            rmdir scripts 2>/dev/null || true
            chmod +x deploy.sh

      - name: Deploy on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            cd ~/apps/csodaszarvas-booking-staging
            APP_DIR="$HOME/apps/csodaszarvas-booking-staging" IMAGE_NAME="csodaszarvas-booking:staging" IMAGE_FILE="csodaszarvas-booking-staging.tar" COMPOSE_FILE="docker-compose.staging.yml" ./deploy.sh

      - name: Clean up local artifacts
        if: always()
        run: rm -f csodaszarvas-booking-staging.tar

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            cd ~/apps/csodaszarvas-booking-staging
            docker compose -f docker-compose.staging.yml ps
            echo "Staging deployment successful! App is running at staging.foglalas.ijaszat.eu"
